#include "main.h"


/**
 * _isupper - checks Write a function that checks for uppercase character.
 * @c: character to check
 *
 * Return: gives 1 or 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 98)
	{
	return(1);
	}
	return(0);
}
